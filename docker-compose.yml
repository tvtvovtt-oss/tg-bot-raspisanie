version: '3.8'

services:
  bot:
    build: .
    container_name: almetpt_schedule_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/app/data
    environment:
      - DATABASE_PATH=/app/data/bot.db

volumes:
  bot_data:
